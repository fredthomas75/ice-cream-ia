// Shared shipping constants — must NOT be marked "use client" so the API route
// (server) and the cart store (client) can both import them.

export const DELIVERY_FEE = 6.5;
export const FREE_DELIVERY_THRESHOLD = 60;
